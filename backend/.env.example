# ===========================================
# Database (required for Prisma)
# ===========================================
# PostgreSQL connection string - used by Prisma for all DB operations.
# Supabase provides PostgreSQL: use the connection string from Supabase Dashboard > Settings > Database
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres"

# ===========================================
# Supabase (required for file uploads)
# ===========================================
# These are DIFFERENT from DATABASE_URL!
# - DATABASE_URL = direct PostgreSQL connection (Prisma)
# - SUPABASE_URL = Supabase API URL (Storage, Auth, Realtime)
# You need BOTH if you use file uploads to Supabase Storage.
#
# Get from: Supabase Dashboard > Settings > API
SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
SUPABASE_SERVICE_KEY="your-service-role-key"

# Storage bucket name for uploads
SUPABASE_BUCKET="documents"

# ===========================================
# Auth & Server
# ===========================================
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
PORT=4000
