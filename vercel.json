{
    "buildCommand": "npm run build --workspaces",
    "outputDirectory": "frontend/dist",
    "rewrites": [
        {
            "source": "/graphql",
            "destination": "/api"
        },
        {
            "source": "/api/(.*)",
            "destination": "/api"
        }
    ]
}